services:
  frontend:
    container_name: frontend
    ports:
      - '${VITE_SERVER_PORT}:${VITE_SERVER_PORT}'
    build:
      dockerfile: Dockerfile
    networks:
      - store-network
networks:
  store-network:
    external: true
